# Python
__pycache__/
*.pyc
.venv/

# Build output (regenerated by build.py)
site/

# Local-only / secrets (never commit credentials)
config.local.toml
.aws/
*.pem

# macOS
.DS_Store

# NOTE: content/images/ is intentionally TRACKED (local image backup).

# privacy — never publish these
ghost_export.json
ghost_export*.json
docs/
infra/route53-backup-*.json
.superpowers/
